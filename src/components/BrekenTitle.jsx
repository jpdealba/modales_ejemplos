import React from "react";

const BrekenTitle = ({ title }) => {
  return (
    <div className="text-center w-full">
      <h1 className="text-[#415364] font-bold text-4xl">{title}</h1>
    </div>
  );
};

export default BrekenTitle;
